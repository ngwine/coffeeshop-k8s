# ============================================================
# CoffeeShop — Production Overlay
# Full replicas, higher resources, production-grade settings
# ============================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base
  - namespace.yaml

labels:
  - pairs:
      environment: production
    includeSelectors: false

# ── Production replicas (also controlled by HPA) ──
replicas:
  - name: backend
    count: 2
  - name: frontend
    count: 2

# ── Patch: production resource limits ──
patches:
  # Backend: production resources
  - target:
      kind: Deployment
      name: backend
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: backend
      spec:
        template:
          spec:
            containers:
              - name: backend
                resources:
                  requests:
                    cpu: 100m
                    memory: 256Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi

  # ConfigMap: production values
  - target:
      kind: ConfigMap
      name: coffeeshop-config
    patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: coffeeshop-config
      data:
        CORS_ORIGINS: "https://coffeeshopk8s.me,https://www.coffeeshopk8s.me"
        FRONTEND_URL: "https://coffeeshopk8s.me"
        API_BASE_URL: "https://coffeeshopk8s.me/api"
        GOOGLE_REDIRECT_URI: "https://coffeeshopk8s.me/api/auth/google/callback"
